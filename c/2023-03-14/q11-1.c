// q 11-1-1 새벽 3시 40분
/*
#include <stdio.h>

int main(void){
    int arr[5], max=0, min=100000000, sum=0;

    for (int i = 0; i < 5; i++)
    {
        scanf("%d", &arr[i]);
    }

    for (int idx = 0; idx < sizeof(arr)/sizeof(int); idx++)
    {
        if(arr[idx] > max)
        {
            max = arr[idx];
        }
        if (arr[idx] < min)
        {
            min = arr[idx];
        }
        sum += arr[idx];
    }
    
    printf("입력된 정수 중에서 최댓값 %d", max);
    printf("입력된 정수 중에서 최소값 %d", min);
    printf("입력된 정수의 총 합%d", sum);
    
    return 0;
}
*/

// q11-1-2 위 문제 10분 소요
